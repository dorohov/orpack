.header-menu {
	position:relative;
	display:flex;
	flex-direction:row;
	//flex-wrap:wrap;
	justify-content:space-between;
	align-content:stretch;
	#site.w(100%);
	#site.h(100%);
	
	.item {
		display:flex;
		flex-direction:row;
		justify-content:space-between;
	}
	
	& > .item:nth-child(1) .item a {
		margin-right:40px;
	}
	& > .item:last-child img {
		margin-top: 15px;
	}
	
	a {
		display:inline-flex;
		align-items: center;
		vertical-align:middle;
		#site.size(18px);
		#site.uppercase();
		#site.h(100%);
	}
	
}